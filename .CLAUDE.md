# FreshGuide Quick Notes

- Home floor room navigation must use `R.id.action_home_to_roomDetail`.
- `HomeFragment` owns overall map + `map_floor_1..5` integration and room binding.
- Admin dashboard includes a dedicated logout button: `btn_admin_logout`.
